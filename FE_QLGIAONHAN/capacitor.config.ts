import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nhigia.logistics',
  appName: 'nhi-gia-logistics',
  webDir: 'dist/nhigia-logistics'
};

export default config;
